- タスクバー点滅だけでなく、Windows 通知も監視して attention 表示できるようにしました。
- 通知から単一のウィンドウに対応付けできた場合、そのサムネイルを赤い点滅枠で強調するようにしました。
- 通知の対応先候補が複数残る場合、その候補サムネイルをオレンジの点滅枠で示すようにしました。
- 未監視ウィンドウも候補絞り込みには利用しつつ、壁面上の強調表示は監視対象だけに限定しました。
- 通知マッチングを見直し、タイトル語や識別子の比較を部分一致ではなく厳密一致ベースに絞り込みました。
- OS 通知を attention 表示に反映するかどうかを保持する Settings 項目を追加し、既定値はオフにしました。
- `build-msix.ps1` を更新し、ローカル検証用の自己署名テストパッケージを再利用できる形で生成するようにしました。
- 最新のバンドルをインストールし、証明書取り込みとアプリ起動まで行う MSIX インストール補助スクリプトを追加しました。
- ビルド、インストール、起動を 1 回で行える `build-and-run-msix.ps1` を追加しました。
- 実行中のデスクトップウィンドウが汎用ドキュメントアイコンではなく、意図したアプリアイコンを維持するよう修正しました。
